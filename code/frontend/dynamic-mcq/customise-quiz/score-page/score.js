const jsConfetti = new JSConfetti();

( jsConfetti.addConfetti({
    confettiColors: [
        '#E6ECEF', '#181887', '#0FD7DF', '#fff', '#A6B2FC', '#9EF0FF' , '#716AFF', '#fdff6a', '#ff718d',
      ],
      emojiSize: 40,
      confettiNumber: 500,
    })
  );